export class registerApiRequest{
    // first_name ?: string;
    // last_name ?:string;
    email ?:string;
    password ?:string;
    // confirm_password ?:string
}

export class loginApiRequest{
    username ?:string;
    password ?:string;
}